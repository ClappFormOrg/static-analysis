// The scanner is deliberately dependency-free, so running it never needs a
// module download and it works on a branch that does not compile.
module github.com/ClappFormOrg/static-analysis

go 1.27.1
